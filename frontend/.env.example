# Copy to .env.local for local dev, or set in Vercel / host dashboard.
# Must match your deployed backend origin (no trailing slash).

NEXT_PUBLIC_API_URL=http://localhost:4000
# Eslatma: Socket.io avtomatik ravishda shu origin dan ulanadi (NEXT_PUBLIC_WS_URL kerak emas).
# Vercelda eski NEXT_PUBLIC_WS_URL bo‘lsa — o‘chirib tashlang yoki API bilan bir xil host qiling.

# Shaxsiy chat headerida ovoz/video qo'ng'iroq (false = yashirish)
NEXT_PUBLIC_CHAT_CALLS_ALLOWED=true

NEXT_PUBLIC_LIVEKIT_URL=wss://your-livekit-host

# Optional: Telegram deep links (register page)
NEXT_PUBLIC_TELEGRAM_BOT_USERNAME=
NEXT_PUBLIC_TELEGRAM_BOT_URL=

# Optional: Hugging Face token for summarize feature
NEXT_PUBLIC_HF_TOKEN=

# Server-only in Next API route (translate)
# LIBRETRANSLATE_URL=http://localhost:5000
