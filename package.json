{
  "name": "messenjrali",
  "private": true,
  "description": "Frontend + backend (MessenjrAli / Mali)",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently -n api,web -c cyan,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "test": "npm run test --prefix frontend && npm run test --prefix backend && npm run test --prefix mobile-app",
    "test:frontend": "npm run test --prefix frontend",
    "test:backend": "npm run test --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
